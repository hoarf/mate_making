<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= f.label :skill_list %>
  </div>
  <div>
    <%= f.text_field :skill_list , :class => "tag_field" %>
  </div>
  <div>
    <%= f.label :interest_list %>
  </div>
  <div>
    <%= f.text_field :interest_list , :class => "tag_field" %>
  </div>
 <div class="actions">
    <%= f.submit "mm.submit()" %>
 </div>
<% end %>